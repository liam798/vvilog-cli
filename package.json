{
  "name": "vvilog",
  "version": "0.2.0",
  "description": "VviLog CLI for configuration, updates, skills and raw API requests",
  "license": "Apache-2.0",
  "bin": {
    "vvilog": "./bin/vvilog.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node tests/run-tests.js"
  }
}
